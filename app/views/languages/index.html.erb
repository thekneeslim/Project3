<div class="container">
  <p id="notice"><%= notice %></p>
  <%= render "partials/verticleColumn" %>

  <!-- ============ FIRST EDIT BOX ============ -->
  <div class="offset-md-1 col-md-8">
    <h3 id="titleHeader">Lanugage Proficiency</h3>
    <div class="card">
      <div class="card-header">
        Languages
      </div>
      <div class="card-block">
        <table>
          <tr>
            <th class="tableHeaderWidth">Languages:</th>
            <% @student.languages.each do |language| %>
            <td><%= language.name %></td>
            <%end%>
          </tr>
        </tbody>
      </table>
      <button type="button" class="btn pinkColour editButton" data-toggle="modal" data-target="#courseInfoEdit">
        Edit
      </button>
    </div>

    <!-- MODAL -->
    <div class="modal fade" id="courseInfoEdit" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
            <h4 class="modal-title" id="myModalLabel">Course Information</h4>
          </div>
          <!-- ============ FORM ============ -->
          <%= form_for(@student) do |f| %>

          <div class="modal-body">
            <div class="form-group row field">
              <%= f.label :language, class: "col-xs-3 col-form-label" %>
              <%= f.select :language_ids, Language.all.collect {|x| [x.name, x.id]}, {},  :multiple => true, class: "col-xs-7 form-control selectForm" %>
            </div>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            <button type="submit" class="btn pinkColour">Save changes</button>
          </div>
          <!-- ============ FORM END ============ -->
          <%end%>
        </div>
      </div>
    </div>
    <!-- ============ MODAL END ============ -->
  </div>
</div>



<!-- <p id="notice"><%= notice %></p>

<h1>Languages</h1>

<div class="container-fluid">
  <%= render "partials/verticleColumn" %>

    <div class="card offset-md-1 col-md-8">
      Happy land
    </div>
</div>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @languages.each do |language| %>
      <tr>
        <td><%= language.name %></td>
        <td><%= link_to 'Show', language %></td>
        <td><%= link_to 'Edit', edit_language_path(language) %></td>
        <td><%= link_to 'Destroy', language, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Language', new_language_path %> -->
